services:
  synthezia:
    # Build from local Dockerfile; replace with `image: ghcr.io/matvd/synthezia:latest`
    # if you prefer pulling a prebuilt image.
    build:
      context: .
      dockerfile: Dockerfile.cuda
    image: synthezia:local-cuda
    container_name: synthezia
    ports:
      - "8080:8080"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities:
                - gpu
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
    # environment:
    #   - HOST=0.0.0.0
    #   - PORT=8080
    #   - DATABASE_PATH=/app/data/synthezia.db
    #   - UPLOAD_DIR=/app/data/uploads
      - PUID=${PUID:-10001}
      - PGID=${PGID:-10001}
    volumes:
      - ./synthezia_data:/app/data
      - ./env-data:/app/whisperx-env
    restart: unless-stopped

volumes:
  synthezia_data:
  env-data:
